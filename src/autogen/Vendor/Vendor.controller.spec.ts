/**
 * This file generated by Apso.
 */
import { Test, TestingModule } from '@nestjs/testing';
import { Vendor, VendorCreate } from './dtos/Vendor.dto';
import { VendorController } from './Vendor.controller';
import { VendorService } from './Vendor.service';
import { CrudRequest } from '@nestjsx/crud';

const baseRequest: CrudRequest = {
  parsed: {
    fields: [],
    paramsFilter: [],
    search: {},
    filter: [],
    or: [],
    join: [],
    sort: [],
    authPersist: undefined,
    limit: 10,
    offset: 0,
    page: 1,
    cache: undefined,
  },
  options: {},
};

describe('VendorController', () => {
  let controller: VendorController;
  let spyService: VendorService;

  beforeEach(async () => {
    const ApiServiceProvider = {
      provide: VendorService,
      useFactory: () => ({
        createOne: jest.fn(() => []),
        getOne: jest.fn(() => {}),
      }),
    };
    const module: TestingModule = await Test.createTestingModule({
      controllers: [VendorController],
      providers: [VendorService, ApiServiceProvider],
    }).compile();

    controller = module.get<VendorController>(VendorController);
    spyService = module.get<VendorService>(VendorService);
  });

  it('should be defined', () => {
    expect(controller).toBeDefined();
  });

  it('should call VendorController get method', () => {
    const req: CrudRequest = { ...baseRequest };
    expect(controller.get(req)).not.toEqual(null);
    expect(spyService.getOne).toHaveBeenCalled();
  });

  it('should call VendorController create method', () => {
    const req: CrudRequest = { ...baseRequest };

    const dto = new Vendor();
    controller.create(req, dto);
    expect(controller.create(req, dto)).not.toEqual(null);
    expect(spyService.createOne).toHaveBeenCalled();
  });

  // Add your tests here
});
